import * as React from 'react';
import type { PanelMode } from '../interface';
export default function getExtraFooter(prefixCls: string, mode: PanelMode, renderExtraFooter?: (mode: PanelMode) => React.ReactNode): React.JSX.Element;
