import type React from 'react';
import type { DescriptionsItemType } from '..';
declare const useRow: (mergedColumn: number, items?: DescriptionsItemType[], children?: React.ReactNode) => DescriptionsItemType[][];
export default useRow;
